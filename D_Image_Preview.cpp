#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'
#define tc     \
    ll tc;     \
    cin >> tc; \
    while (tc--)
#define pb push_back
#define mp make_pair
const ll MOD = 1e9 + 7;

void fastio()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

void solve()
{
    int n,a,b;
    cin >> n >> a >> b;//n = number of images, a = time to move to next image, b = time to change the orientaion of the image.
    ll T;cin>> T;
    string s;
    cin >> s;
    ll ans = 0;
    



}
int findCost(string s, int a, int b, int n){
    
}

int main()
{
    fastio();
    ll t;
    cin >> t;
    for (ll i = 1; i <= t; i++)
    {
        solve();
    }

    return 0;
}