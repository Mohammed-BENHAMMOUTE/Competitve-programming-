#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

void setIO(string name = "")
{
  ios::sync_with_stdio();
  cin.tie(NULL);
  if(!name.empty())
  {
    freopen((name + "in").c_str(),"r", stdin);
    freopen((name+ "out").c_str(), "w", stdout);
  }
};




void solve()
{

}

int main()
{
  int t =1; 
  cin >> t;
  while (t--) {
    solve();
  }
  return 0;
}
